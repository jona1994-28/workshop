<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Node.js</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">     <!-- Menú -->
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <%- include('./partials/header') %>
    <main class="container">
        <h1>Usuarios</h1>
        <a href="/users/create" class="btn btn-sm btn-primary">Crear</a>
        <table class="table table-sprited"><!-- cada fila tiene un color diferente -->
            <thead> <!-- cabezera -->
                <tr>
                    <th>#</th>
                    <th>Correo</th>
                    <th>Password</th>
                    <th></th>
                </tr>
            </thead>    <!-- cuerpo -->
            <tbody>
                <% rows.forEach(users => { %>    <!-- se escribe asi porque sino no me toma el html que viene despúes. -->
                    <tr>
                        <td><%= users.id %></td>    <!-- sirve para imprimir el objeto -->
                        <td><%= users.email %></td>
                        <td><%= users.password %></td>
                    </tr>
                <%  }) %>
            </tbody>
        </table>
    </main>
    <%- include('./partials/footer') %>
</body>
</html>